version: "3.1"

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n_github
    ports:
      - "5678:5678"
    environment:
      - N8N_ENCRYPTION_KEY=
    volumes:
      - ./data:/home/node/.n8n
    restart: always
